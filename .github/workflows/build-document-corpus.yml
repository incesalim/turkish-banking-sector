name: Build audit document corpus

# Separate from analytical snapshot publication. Stores immutable originals and
# source evidence only; a successful run never means the contents are verified.
on:
  workflow_run:
    workflows: ["Refresh audit reports", "Acquire audit reports"]
    types: [completed]
    branches: [master]
  workflow_dispatch:
    inputs:
      banks:
        description: "ALL or comma-separated registered bank tickers"
        type: string
        default: "ALL"
      period:
        description: "One YYYYQn; blank = all registered periods"
        type: string
        default: ""
      limit:
        description: "Maximum filings after filters; 0 = all"
        type: number
        default: 0
      publish:
        description: "Preserve originals and page evidence in the separate R2 corpus"
        type: boolean
        default: true
      structure:
        description: "Add source-linked tables, paragraphs and headings (unverified)"
        type: boolean
        default: true
      quality_only:
        description: "Read-only review of preserved PDF bytes, source identity, text legibility and recovery gaps"
        type: boolean
        default: false
      acquire_missing:
        description: "Fill only missing explicit registry sources before capture; requires publish=true"
        type: boolean
        default: false
      recheck_bytes:
        description: "Read every selected artifact again, bypassing unchanged-object receipts"
        type: boolean
        default: false
      kind:
        description: "Filing basis"
        type: choice
        options: [BOTH, consolidated, unconsolidated]
        default: BOTH
      ocr_pages:
        description: "Read-only OCR probe: up to four PDF pages; requires publish=false and limit=1..4"
        type: string
        default: ""
      ocr_dpi:
        description: "OCR probe rendering resolution"
        type: choice
        options: ["300", "450", "600"]
        default: "300"
      ocr_language:
        description: "OCR probe languages (pinned model files)"
        type: choice
        options: [eng+tur, tur+eng, eng, tur]
        default: eng+tur
      vector_pages:
        description: "Read-only outline probe: up to four PDF pages; requires publish=false and limit=1..4"
        type: string
        default: ""
      review_pages:
        description: "Visual source review: up to four ordered PDF pages, or ALL for limit=1; requires publish=false"
        type: string
        default: ""

permissions:
  contents: read

concurrency:
  # Read-only probes can inspect new code while a source-preservation fleet runs.
  # All publishing runs still share one queue.
  group: ${{ github.event_name == 'workflow_dispatch' && (!inputs.publish || inputs.quality_only) && format('audit-document-probe-{0}', github.run_id) || 'audit-document-corpus' }}
  queue: max
  cancel-in-progress: false

jobs:
  acquire:
    if: github.event_name == 'workflow_dispatch' && inputs.acquire_missing && inputs.publish && !inputs.quality_only
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        shard: ${{ fromJSON(((inputs.banks == 'ALL' || inputs.banks == '') && inputs.limit == 0) && '[0,1,2,3]' || '[0]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      PYTHONIOENCODING: utf-8
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Acquire only missing registered sources
        env:
          BANKS: ${{ inputs.banks || 'ALL' }}
          PERIOD: ${{ inputs.period }}
          KIND: ${{ inputs.kind || 'BOTH' }}
          LIMIT: ${{ inputs.limit || 0 }}
          SHARD_INDEX: ${{ matrix.shard }}
          SHARD_COUNT: ${{ strategy.job-total }}
        run: |
          args=(--limit "$LIMIT" --shard-count "$SHARD_COUNT" --shard-index "$SHARD_INDEX")
          [ "$BANKS" != "ALL" ] && args+=(--bank "$BANKS")
          [ -n "$PERIOD" ] && args+=(--period "$PERIOD")
          [ "$KIND" != "BOTH" ] && args+=(--kind "$KIND")
          python scripts/acquire_document_corpus.py "${args[@]}"
      - name: Keep every acquisition outcome
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: audit-document-acquisition-report-${{ matrix.shard }}
          path: data/audit_capture/corpus-v1/acquisition-results.json
          if-no-files-found: warn
          retention-days: 30

  capture:
    # All acquisition groups finish before any capture group snapshots inventory.
    # Successful sources still get preserved when another acquisition needs review.
    needs: acquire
    if: always() && !cancelled() && (github.event_name == 'workflow_dispatch' || github.event.workflow_run.head_repository.full_name == github.repository)
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        # Full scope uses disjoint filing hashes. Bounded probes stay one job.
        shard: ${{ fromJSON((github.event_name != 'workflow_dispatch' || ((inputs.banks == 'ALL' || inputs.banks == '') && inputs.limit == 0)) && '[0,1,2,3]' || '[0]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 360
    env:
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      PYTHONIOENCODING: utf-8
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Validate acquisition scope
        if: inputs.acquire_missing && (!inputs.publish || inputs.quality_only)
        run: |
          echo 'Missing-source acquisition requires a publishing capture, not a read-only review.'
          exit 1
      - name: Validate visual review scope
        if: github.event_name == 'workflow_dispatch' && inputs.review_pages != '' && (inputs.publish || inputs.quality_only)
        run: |
          echo 'Visual source review requires a read-only capture, not publication or quality-only review.'
          exit 1
      - name: Preserve and verify source artifact bytes
        if: ${{ !inputs.quality_only }}
        env:
          BANKS: ${{ inputs.banks || 'ALL' }}
          PERIOD: ${{ inputs.period }}
          LIMIT: ${{ inputs.limit || 0 }}
          PUBLISH: ${{ github.event_name != 'workflow_dispatch' || inputs.publish }}
          STRUCTURE: ${{ github.event_name != 'workflow_dispatch' || inputs.structure }}
          RECHECK_BYTES: ${{ inputs.recheck_bytes || false }}
          SHARD_INDEX: ${{ matrix.shard }}
          SHARD_COUNT: ${{ strategy.job-total }}
          KIND: ${{ inputs.kind || 'BOTH' }}
          OCR_PAGES: ${{ inputs.ocr_pages }}
          OCR_DPI: ${{ inputs.ocr_dpi || '300' }}
          OCR_LANGUAGE: ${{ inputs.ocr_language || 'eng+tur' }}
          VECTOR_PAGES: ${{ inputs.vector_pages }}
          REVIEW_PAGES: ${{ inputs.review_pages }}
        run: |
          args=(--from-r2 --capture --limit "$LIMIT" --shard-count "$SHARD_COUNT" --shard-index "$SHARD_INDEX")
          [ "$BANKS" != "ALL" ] && args+=(--bank "$BANKS")
          [ -n "$PERIOD" ] && args+=(--period "$PERIOD")
          [ "$KIND" != "BOTH" ] && args+=(--kind "$KIND")
          [ -n "$OCR_PAGES" ] && args+=(--ocr-pages "$OCR_PAGES" --ocr-dpi "$OCR_DPI" --ocr-language "$OCR_LANGUAGE")
          [ -n "$VECTOR_PAGES" ] && args+=(--vector-pages "$VECTOR_PAGES")
          [ -n "$REVIEW_PAGES" ] && args+=(--review-pages "$REVIEW_PAGES")
          [ "$PUBLISH" = "true" ] && args+=(--publish --discard-published)
          [ "$STRUCTURE" = "true" ] && args+=(--structure)
          [ "$RECHECK_BYTES" = "true" ] && args+=(--recheck-bytes)
          python scripts/build_document_corpus.py "${args[@]}"
      - name: Review source identity, retained bytes and unresolved content
        if: inputs.quality_only
        env:
          BANKS: ${{ inputs.banks || 'ALL' }}
          PERIOD: ${{ inputs.period }}
          KIND: ${{ inputs.kind || 'BOTH' }}
          LIMIT: ${{ inputs.limit || 0 }}
          SHARD_INDEX: ${{ matrix.shard }}
          SHARD_COUNT: ${{ strategy.job-total }}
        run: |
          args=(--limit "$LIMIT" --shard-count "$SHARD_COUNT" --shard-index "$SHARD_INDEX")
          [ "$BANKS" != "ALL" ] && args+=(--bank "$BANKS")
          [ -n "$PERIOD" ] && args+=(--period "$PERIOD")
          [ "$KIND" != "BOTH" ] && args+=(--kind "$KIND")
          python scripts/review_document_corpus.py "${args[@]}"
      - name: Keep inventory and named outcomes, including failures
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ strategy.job-total > 1 && format('audit-document-corpus-report-{0}', matrix.shard) || 'audit-document-corpus-report' }}
          path: |
            data/audit_capture/corpus-v1/inventory.json
            data/audit_capture/corpus-v1/capture-results.json
            data/audit_capture/corpus-v1/quality-results.json
          if-no-files-found: warn
          retention-days: 30
      - name: Keep bounded read-only probe evidence for independent review
        if: always() && github.event_name == 'workflow_dispatch' && !inputs.quality_only && !inputs.publish && inputs.limit > 0 && inputs.limit <= 4
        uses: actions/upload-artifact@v7
        with:
          name: audit-document-probe-evidence
          path: data/audit_capture/corpus-v1/sources/
          if-no-files-found: warn
          retention-days: 7
